{
  "framework": null,
  "installCommand": "rm -rf node_modules && npm install && npx prisma format && npx prisma validate && npx prisma generate",
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "routes": [
    {
      "src": "^/api/(.*)$",
      "dest": "/api/$1"
    },
    {
      "src": "^(?!/api/).*",
      "dest": "/server/index.js"
    }
  ],
  "crons": [
    {
      "path": "/api/cron/praxisbericht-reminder",
      "schedule": "0 17 * * 5"
    },
    {
      "path": "/api/cron/daily-reminders",
      "schedule": "0 18 * * *"
    }
  ]
}
